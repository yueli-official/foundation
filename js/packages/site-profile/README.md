# `@yueli/site-profile`

Headless management Adapter for the Foundation Site Profile contract.

It validates the server FormSchema and Snapshot, provides schema-owned field
lookup, safe dotted-path access, dirty/reset state and an `If-Match`
replacement request. The optional Vue export wraps the same editor; products
retain their layout and authorization.
