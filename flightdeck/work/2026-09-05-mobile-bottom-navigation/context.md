# Context

用户已认可 BVideo 手机样稿并明确授权提交和创建通用组件。本工作拥有 Foundation 通用能力，BVideo 迁移有自己的产品 Work。Workspace 合同只读。

保持“小而美”：内容高48px、边框1px、安全区另计、图标20px、文字11px，768px起隐藏并释放占位。一个文档只放一个全局移动导航。输入聚焦时隐藏导航但保留占位，吸底业务操作用 --yueli-mobile-bottom-space 避让；导航通过 data-y-dock 接入 BackToTop，Toast直接消费共享变量。

0.4.0 尚未发布，本次兼容新增并入候选；不改变已发布制品URL，不推送/发布。保留现有未跟踪 workspace/。
