// Package discovery compiles product-owned public page facts into deterministic
// search, social, structured-data, sitemap, feed and robots projections.
//
// Discovery is instance-local and framework-neutral. It never owns product
// publication state, trusts request Host headers, or treats robots.txt as
// authorization.
package discovery
